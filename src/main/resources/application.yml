server:
  port: 9001

spring:
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    generate-ddl: true
    hibernate:
      ddl-auto: create
      jdbc:
        batch_size: 1000

  datasource:
    url: jdbc:mysql://localhost:3307/sunlife
    username: maninder
    password: maninder
    driver-class-name: com.mysql.cj.jdbc.Driver

kafka:
  bootstrap-server: localhost:9092
  schema-registry: http://localhost:8081
  application-id: phone-app