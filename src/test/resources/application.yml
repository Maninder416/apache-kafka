spring:

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
    generate-ddl: true
    hibernate:
      ddl-auto: create
      jdbc:
        batch_size: 1000

  kafka:
    bootstrap-server: localhost:9092
    producer:
      keySerializer: "org.apache.kafka.common.serialization.StringSerializer"
      valueSerializer: "io.confluent.kafka.serializers.KafkaAvroSerializer"
      properties:
        schema:
          registry:
            url: http://localhost:8081

    consumer:
      group-id: "employee-details-app"
      keyDeserializer: "org.apache.kafka.common.serialization.StringDeserializer"
      valueDeserializer: "io.confluent.kafka.serializers.KafkaAvroDeserializer"
      properties:
        schema:
          registry:
            url: http://localhost:8081
        specific:
          avro:
            reader: "true"


avro:
  topic:
    employee-basic-details: avro-employee-basic-details
    employee-employment-details: avro-employee-employment-details
    employee-all-details: avro-employee-all-details


